node_modules/
out/
dist/
*.log
.DS_Store
# local SQLite dev database & backups
*.db
*.db-wal
*.db-shm
backups/
# licensing private key — MUST never be committed or shipped
license-private.pem
tests/smoke/shots/
docs/demo/*.webm
# Generated importer fixtures — reproducible with `npm run samples`, so the
# binaries stay out of git while docs/samples/README.md (which explains them)
# stays in.
docs/samples/*.xlsx
docs/samples/*.csv
